import LoginFormHeader from "@/components/forms/login/FormHeader";

const LoginFormImage: React.FC = () => {
  return (
    <div className="w-full h-full bg-primary-600 flex flex-col justify-center px-16">
      <LoginFormHeader />
    </div>
  );
};

export default LoginFormImage;
